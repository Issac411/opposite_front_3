{% extends 'base.html.twig' %}

{% block title %}Hello PolitiqueController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="container fontA styleBlocCentral">
    <div class="barreBlocElement"><button type="button" class="btn btn-info"><a  href="{{ path('traitviewall')}}">Retour</a></button><button type="button" class="btn btn-info"><a  href="{{ path('traitedit', {id:trait.id})}}">Editer</a></button><button type="button" class="btn btn-info"><a  href="{{ path('traitremove', {id:trait.id})}}">Détruire</a></button></div>
    <div class="container p-4 text-justify">
        <div><h3  style="color:{{ trait.couleur }};" >{{ trait.Libelle }}</h3></div>
        <div>{{ trait.Couleur }}</div>
    </div>

</div>
{% endblock %}
