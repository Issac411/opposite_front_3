{% extends 'base.html.twig' %}

{% block title %}Hello PolitiqueController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="container fontA styleBlocCentral">
    <div class="barreBlocElement"><button type="button" class="btn btn-primary m-2"><a  href="{{ path('nationBuild')}}">Ajouter un pays</a></button></div>

    <h3>Nations</h3>
    <div class="container">
        {% for nation in nations %}
            <tr class="">
                <td> <a href="{{ path('nationview', {id:nation.id}) }}"> {{ nation.Libelle }}</a></td>
                <td><img height="24px" width="24px" src="{{ nation.drapeau }}"/></td>
            </tr>
        {% endfor %}

    </div>


</div>
{% endblock %}
